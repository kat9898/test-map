export const activeRowDetailsSelector = (state) => state.activeRow.rowDetails;

export const activeRowSelector = (state) => state.activeRow;

export const polylineSelector = (state) => state.requestPolyline.data;

export const urlSelector = (state) => state.activeRow.polylineUrl;